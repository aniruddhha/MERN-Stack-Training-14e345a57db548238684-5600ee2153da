class Car {
    // properties
    // behaviour --> act on propertes

    speed = 10; // property

    speedUp() { // behaviour
        this.speed = this.speed + 10
    }
}

const cr = new Car() // real object