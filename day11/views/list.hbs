<div class="container">
    <div class="row">
        <div class="col">
            {{#each mobiles}}
            <div>{{price}} - {{os}}</div>
            {{/each}}
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{#foo mobiles.length 4}}
            <p> in if success </p>
            {{else}}
            <p> in else</p>
            {{/foo}}
        </div>
    </div>
</div>